package com.alpara.beus.Firebase.Auth

actual fun initializePlatform() {
    // Firebase is automatically initialized on iOS via GoogleService-Info.plist
    // No additional initialization needed for GitLive Firebase
}
